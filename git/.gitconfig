[alias]
  co = checkout
  st = status
  hist = log --pretty=format: '%h %ad | %s%d [%an]' --graph  --date=short
  type = cat-file -t
  dump = cat-file -p
  s = status --short
  sb = show-branch
  sbr = show-branch --remotes
  l = log --oneline
  r = remote
[color]
  ui = true
  
